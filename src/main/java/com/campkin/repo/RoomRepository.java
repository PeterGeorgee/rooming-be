package com.campkin.repo; import com.campkin.domain.Room; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface RoomRepository extends JpaRepository<Room,UUID>{ List<Room> findByCampIdOrderByName(UUID campId); }
