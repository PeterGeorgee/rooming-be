package com.campkin.repo; import com.campkin.domain.RoomLeader; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface RoomLeaderRepository extends JpaRepository<RoomLeader,UUID>{ List<RoomLeader> findByManagedRoomIdOrderByName(UUID roomId); List<RoomLeader> findByManagedRoomCampId(UUID campId); long countBySleepRoomId(UUID roomId); void deleteByManagedRoomId(UUID roomId); }
